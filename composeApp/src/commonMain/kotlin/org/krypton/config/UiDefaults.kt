package org.krypton.config

/**
 * Default configuration values for UI components.
 * 
 * These defaults are used when user settings are missing or invalid.
 * All values are in pixels unless otherwise noted.
 */
object UiDefaults {
    // Ribbon dimensions
    /**
     * Default ribbon width in pixels.
     */
    const val DEFAULT_RIBBON_WIDTH = 48
    
    // Sidebar dimensions
    /**
     * Default sidebar minimum width in pixels.
     */
    const val DEFAULT_SIDEBAR_MIN_WIDTH = 200
    
    /**
     * Default sidebar default width in pixels.
     */
    const val DEFAULT_SIDEBAR_DEFAULT_WIDTH = 280
    
    /**
     * Default sidebar maximum width in pixels.
     */
    const val DEFAULT_SIDEBAR_MAX_WIDTH = 400
    
    // Tab dimensions
    /**
     * Default tab height in pixels.
     */
    const val DEFAULT_TAB_HEIGHT = 36
    
    /**
     * Default tab padding in pixels.
     */
    const val DEFAULT_TAB_PADDING = 12
    
    /**
     * Default tab corner radius in pixels.
     */
    const val DEFAULT_TAB_CORNER_RADIUS = 6
    
    // Panel dimensions
    /**
     * Default panel border width in pixels.
     */
    const val DEFAULT_PANEL_BORDER_WIDTH = 1
    
    /**
     * Default panel padding in pixels.
     */
    const val DEFAULT_PANEL_PADDING = 8
    
    // Font sizes
    /**
     * Default tab font size in pixels.
     */
    const val DEFAULT_TAB_FONT_SIZE = 13
    
    /**
     * Default tab label font size in pixels.
     */
    const val DEFAULT_TAB_LABEL_FONT_SIZE = 11
    
    /**
     * Default file explorer font size in pixels.
     */
    const val DEFAULT_FILE_EXPLORER_FONT_SIZE = 14
    
    // Icon sizes
    /**
     * Small icon size in pixels.
     */
    const val DEFAULT_ICON_SIZE_SMALL = 16
    
    /**
     * Medium icon size in pixels.
     */
    const val DEFAULT_ICON_SIZE_MEDIUM = 20
    
    /**
     * Large icon size in pixels.
     */
    const val DEFAULT_ICON_SIZE_LARGE = 24
    
    // Sidebar spacing
    /**
     * Default sidebar item height in pixels.
     */
    const val DEFAULT_SIDEBAR_ITEM_HEIGHT = 30
    
    /**
     * Default sidebar indent per level in pixels.
     */
    const val DEFAULT_SIDEBAR_INDENT_PER_LEVEL = 12
    
    /**
     * Default sidebar horizontal padding in pixels.
     */
    const val DEFAULT_SIDEBAR_HORIZONTAL_PADDING = 8
    
    /**
     * Default sidebar vertical padding in pixels.
     */
    const val DEFAULT_SIDEBAR_VERTICAL_PADDING = 5
    
    /**
     * Default spacing between icon and text in sidebar in pixels.
     */
    const val DEFAULT_SIDEBAR_ICON_TEXT_SPACING = 6
    
    /**
     * Default chevron width in sidebar in pixels.
     */
    const val DEFAULT_SIDEBAR_CHEVRON_WIDTH = 20
    
    /**
     * Default sidebar separator height in pixels.
     */
    const val DEFAULT_SIDEBAR_SEPARATOR_HEIGHT = 1
    
    /**
     * Default sidebar section height in pixels.
     */
    const val DEFAULT_SIDEBAR_SECTION_HEIGHT = 40
    
    /**
     * Default spacing between items in sidebar in pixels.
     */
    const val DEFAULT_SIDEBAR_ITEM_SPACING = 8
    
    /**
     * Default ribbon unit size in pixels.
     */
    const val DEFAULT_RIBBON_UNIT = 32
    
    // Animation durations (milliseconds)
    /**
     * Default animation duration for UI transitions in milliseconds.
     */
    const val DEFAULT_ANIMATION_DURATION_MS = 200L
    
    /**
     * Default short animation duration in milliseconds.
     */
    const val DEFAULT_SHORT_ANIMATION_DURATION_MS = 100L
    
    /**
     * Default long animation duration in milliseconds.
     */
    const val DEFAULT_LONG_ANIMATION_DURATION_MS = 300L
}

